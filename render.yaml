services:
  - type: web
    name: notes-private-call-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ASSISTANT_KEY
        sync: false
