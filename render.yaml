services:
  - type: web
    name: notes-performance-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ASSISTANT_KEY
        sync: false
