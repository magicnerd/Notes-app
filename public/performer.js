const code=new URLSearchParams(location.search).get('room')||'';let ws,mediaStream,audioCtx,processor,micOn=false,noteCache=localStorage.getItem('lastNote')||'';const $=s=>document.querySelector(s);$('#startNote').value=noteCache;function connect(){ws=new WebSocket((location.protocol==='https:'?'wss://':'ws://')+location.host);ws.onopen=()=>ws.send(JSON.stringify({type:'join',role:'performer',code}));ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==='joined'){setConn('Connected',true);if(m.room.current_note){setNote(m.room.current_note)}else if($('#startNote').value.trim()){sendNote($('#startNote').value)}}if(m.type==='note-update')setNote(m.note);if(m.type==='presence')setConn(`P:${m.performers} H:${m.helpers}`,true);if(m.type==='error')setConn(m.error,false)};ws.onclose=()=>{setConn('Reconnecting...',false);setTimeout(connect,1000)}}function setConn(t,ok){$('#conn').textContent=t;$('#conn').className=ok?'status ok':'status bad';$('#setupStatus').textContent=t}function setNote(t){$('#note').value=t;noteCache=t;localStorage.setItem('lastNote',t)}function sendNote(t){setNote(t);if(ws?.readyState===1)ws.send(JSON.stringify({type:'note-update',note:t}))}async function start(withMic){$('#setup').classList.add('hidden');$('#app').classList.remove('hidden');setNote($('#startNote').value||noteCache||'');connect();if(withMic)await startMic()}async function startMic(){try{mediaStream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}});audioCtx=new (window.AudioContext||window.webkitAudioContext)();const src=audioCtx.createMediaStreamSource(mediaStream);processor=audioCtx.createScriptProcessor(4096,1,1);processor.onaudioprocess=e=>{if(!micOn||ws?.readyState!==1)return;const input=e.inputBuffer.getChannelData(0);const pcm=downsample(input,audioCtx.sampleRate,16000);ws.send(JSON.stringify({type:'audio-pcm',sampleRate:16000,data:btoa(String.fromCharCode(...new Uint8Array(pcm.buffer))) }))};src.connect(processor);processor.connect(audioCtx.destination);micOn=true;sendAudioStatus();}catch(e){setConn('Mic blocked or unavailable',false)}}function downsample(buffer,from,to){if(from===to){const out=new Int16Array(buffer.length);for(let i=0;i<buffer.length;i++)out[i]=Math.max(-1,Math.min(1,buffer[i]))*32767;return out}const ratio=from/to,len=Math.round(buffer.length/ratio),out=new Int16Array(len);let pos=0;for(let i=0;i<len;i++){const next=Math.round((i+1)*ratio);let sum=0,c=0;for(;pos<next&&pos<buffer.length;pos++){sum+=buffer[pos];c++}out[i]=Math.max(-1,Math.min(1,sum/(c||1)))*32767}return out}function sendAudioStatus(){if(ws?.readyState===1)ws.send(JSON.stringify({type:'audio-status',on:micOn}))}function stopMic(){micOn=false;if(mediaStream)mediaStream.getTracks().forEach(t=>t.stop());mediaStream=null;sendAudioStatus()}$('#startBtn').onclick=()=>start(true);$('#noMicBtn').onclick=()=>start(false);$('#note').oninput=e=>sendNote(e.target.value);$('#doneBtn').onclick=async()=>{if(micOn){stopMic();$('#doneBtn').textContent='Edit'}else{await startMic();$('#doneBtn').textContent='Done'}};$('#backBtn').onclick=()=>{$('#noteView').classList.add('hidden');$('#list').classList.remove('hidden')};$('#openReal').onclick=()=>{$('#list').classList.add('hidden');$('#noteView').classList.remove('hidden')};let taps=0;document.body.addEventListener('click',e=>{if(e.target.closest('button,textarea'))return;taps++;setTimeout(()=>taps=0,500);if(taps>=3){$('#black').classList.toggle('hidden');taps=0}});document.addEventListener('visibilitychange',()=>{if(!document.hidden&&ws?.readyState!==1)connect()});
