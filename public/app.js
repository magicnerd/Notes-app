let sb, session;
const $=s=>document.querySelector(s);
const api=async(path,opts={})=>{const token=(await sb.auth.getSession()).data.session?.access_token;const r=await fetch(path,{...opts,headers:{'Content-Type':'application/json',...(opts.headers||{}),Authorization:`Bearer ${token}`}});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Request failed');return j};
async function init(){const cfg=await fetch('/config').then(r=>r.json());sb=window.supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);const {data}=await sb.auth.getSession();session=data.session;showAuth();if(session)loadDashboard();}
function showAuth(){$('#auth').classList.toggle('hidden',!!session);$('#dash').classList.toggle('hidden',!session)}
async function signup(){setMsg('Creating account...');const email=$('#email').value.trim();const password=$('#password').value;const code=$('#code').value.trim();const {data,error}=await sb.auth.signUp({email,password});if(error)return setMsg(error.message,true);session=data.session;if(!session){const login=await sb.auth.signInWithPassword({email,password});if(login.error)return setMsg('Account created. Check email if confirmation is required, then log in.',true);session=login.data.session;}if(code){try{await api('/api/activate',{method:'POST',body:JSON.stringify({code})});}catch(e){return setMsg(e.message,true)}}showAuth();loadDashboard();}
async function login(){setMsg('Logging in...');const {data,error}=await sb.auth.signInWithPassword({email:$('#email').value.trim(),password:$('#password').value});if(error)return setMsg(error.message,true);session=data.session;showAuth();loadDashboard();}
async function logout(){await sb.auth.signOut();session=null;location.reload()}
function setMsg(t,bad=false){$('#msg').textContent=t;$('#msg').className=bad?'status bad':'status'}
async function activate(){try{await api('/api/activate',{method:'POST',body:JSON.stringify({code:$('#activateCode').value.trim()})});loadDashboard()}catch(e){$('#activateMsg').textContent=e.message}}
async function createRoom(){const name=prompt('Room name','Performance Room');if(name===null)return;await api('/api/rooms',{method:'POST',body:JSON.stringify({name})});loadDashboard()}
async function resetCodes(id){if(!confirm('Reset performer/helper links for this room? Old links will stop working.'))return;await api(`/api/rooms/${id}/reset-codes`,{method:'POST'});loadDashboard()}
async function deleteRoom(id){if(!confirm('Disable this room?'))return;await api(`/api/rooms/${id}`,{method:'DELETE'});loadDashboard()}
function copy(t){navigator.clipboard.writeText(t)}
async function loadDashboard(){try{const me=await api('/api/me');$('#userEmail').textContent=me.user.email;const activated=!!me.profile?.activated;$('#activation').classList.toggle('hidden',activated);$('#roomsBlock').classList.toggle('hidden',!activated);$('#rooms').innerHTML=(me.rooms||[]).map(r=>`<div class="card"><h3>${escapeHtml(r.name)}</h3><div class="small">Performer link</div><div class="copyBox">${r.performer_url}</div><button onclick="copy('${r.performer_url}')">Copy performer</button><div style="height:10px"></div><div class="small">Helper link</div><div class="copyBox">${r.helper_url}</div><button onclick="copy('${r.helper_url}')">Copy helper</button><button class="ghost" onclick="resetCodes('${r.id}')">Reset links</button><button class="danger" onclick="deleteRoom('${r.id}')">Disable</button></div>`).join('')||'<p>No rooms yet.</p>'}catch(e){setMsg(e.message,true)}}
function escapeHtml(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
window.signup=signup;window.login=login;window.logout=logout;window.activate=activate;window.createRoom=createRoom;window.resetCodes=resetCodes;window.deleteRoom=deleteRoom;window.copy=copy;init();
